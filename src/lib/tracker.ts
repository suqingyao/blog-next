// import { injectConfig } from '@/config';
// import { client } from '@/lib/client';
// TODO 图片浏览上报
export function trackView(photoId: string) {
  console.log('trackView', photoId);
  // if (!injectConfig.useApi)
  //   return;
  // client.actView({
  //   refKey: photoId,
  // });
}
